{
  "name": "admin",
  "version": "1.0.0",
  "main": "expo-router/entry",
  "private": true,
  "scripts": {
    "start": "expo start --port 8091",
    "android": "expo start --android --port 8091",
    "ios": "expo start --ios --port 8091",
    "web": "expo start --web --port 8091",
    "lint": "expo lint",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@myapp/shared": "workspace:*"
  }
}
